import { AuthValidatingAccountComponent } from './auth-validating-account.component'

describe('AuthValidatingAccountComponent', () => {
  it('should mount', () => {
    cy.mount(AuthValidatingAccountComponent)
  })
})