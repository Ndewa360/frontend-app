import { ShowFactureCurrentComponent } from './show-facture-current.component'

describe('ShowFactureCurrentComponent', () => {
  it('should mount', () => {
    cy.mount(ShowFactureCurrentComponent)
  })
})