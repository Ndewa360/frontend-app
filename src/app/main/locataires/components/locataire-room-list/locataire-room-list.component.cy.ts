import { LocataireRoomListComponent } from './locataire-room-list.component'

describe('LocataireRoomListComponent', () => {
  it('should mount', () => {
    cy.mount(LocataireRoomListComponent)
  })
})