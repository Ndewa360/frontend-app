import { AssignLocationStepperComponent } from './assign-location-stepper.component'

describe('AssignLocationStepperComponent', () => {
  it('should mount', () => {
    cy.mount(AssignLocationStepperComponent)
  })
})